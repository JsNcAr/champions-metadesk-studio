"""Champions MetaDesk Studio."""

# Kept equal to the version in pyproject.toml by scripts/release.py (a test checks it).
# Packaged builds cannot read installed-distribution metadata, so this is what they show.
__version__ = "0.2.1"
